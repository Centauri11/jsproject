pipeline {
    agent any
    stages {
        stage('install') {
            steps {
                sh 'npm --version'
                sh 'npm install'
            }
        stage('build') {
            steps {
                sh 'echo Building'
            }


    }
}
